import React from 'react'

import List from './toolkit-list'

const ToolKit = ({ data }) => {
    return (
        <section>
            <List data={data} />
        </section>
    )
}

export default ToolKit
